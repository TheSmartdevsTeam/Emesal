using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObjectStatusScript : MonoBehaviour
{
    // Start is called before the first frame update
    public int _Health;
    public float _Weigth;
    public string _Type;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
